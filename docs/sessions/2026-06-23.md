# 2026-06-23

## v0.2.0 (committed earlier this session)

System tray, configurable global shortcuts, animated "B" logo, clean shutdown
(no orphaned processes), privacy sync, and a `changelog/` directory + in-app
"What's New" view. Verified live: clean boot, and killing the main process tree
leaves 0 Electron processes. Committed on `feature/v0.2.0-tray-shortcuts-changelog`.

## v0.3.0 (this batch)

1. **Sidebar status panel** — replaced the static disclaimer/version text at the
   bottom of the dashboard sidebar with a live status block: profile count,
   session count, and a "Live now" indicator when a session is active. Kept the
   `v<version> · What's new` changelog link.
2. **Custom titlebar** — dashboard window switched to `titleBarStyle: 'hidden'`
   (keeps native resize borders, hides the OS caption) with a custom React
   titlebar: drag region + min / maximize-restore / close controls styled to the
   app. Close hides to tray (existing behavior). New `window:*` IPC + a
   `window:maximized` push event so the maximize/restore icon stays correct.
3. **Reset features** (Settings → Danger zone), both gated by a native confirm:
   - **Reset App Settings** — clears all settings (models, overlay, privacy,
     shortcuts, consent, tour) back to factory defaults; keeps the API key + data.
   - **Remove all user data** — clears the OpenAI API key + deletes all profiles
     and sessions (FK cascade removes documents/notes/jobs/chunks/embeddings/
     transcripts/answers/reports).
   - **Exit hotkey** — new global shortcut `app:quit` (default `Ctrl+Shift+Q`)
     that fully terminates the app + all child processes at once.

### Notes / decisions

- Destructive resets confirm via a native `dialog.showMessageBox` in the main
  process, so they're gated even if the IPC is called directly.
- `app:quit` handler calls `quitApp()` (→ `before-quit` → `performShutdown`), the
  same clean teardown as the tray Exit, so no orphaned processes.
- Used `titleBarStyle: 'hidden'` rather than `frame: false` to preserve native
  window resizing/snapping on Windows.

### Open / needs user's eyes

- GUI pass: maximize/restore button icon sync, drag region, and the two
  destructive resets (confirm + observe data cleared).

## v0.4.0 (this batch) — rebrand + refinements

1. **Renamed app → "BrainCue Copilot"** across installer (electron-builder
   productName/appId `com.braincue.copilot`), `setAppUserModelId`, window title,
   tray, Exit dialog, titlebar, sidebar header, docs. Left npm `name` unchanged
   so the dev userData folder (existing profiles + API key) is preserved.
2. **"Overlay" → "Cue Card"** for all user-facing text (shortcuts, tray, tour,
   Session/Settings copy, overlay close button). Internal `overlay` IPC/identifiers
   left as-is to avoid churn.
3. **Cue Card shown by default** on launch (`showOverlay()` in index.ts); removed
   the sidebar Show/Hide toggle. Toggle via Ctrl+Shift+Space or tray.
4. **Tray menu** relabeled (Show BrainCue Dashboard / Show BrainCue Cue Card /
   Exit BrainCue Copilot) with shortcuts shown via `accelerator` +
   `registerAccelerator:false` (display-only).
5. **Privacy Mode disable is gated** — `requestPrivacy()`/`togglePrivacyGuarded()`
   in privacy.ts show a native confirm when turning OFF; wired into the privacy
   IPC, the global shortcut, and the tray (with checkbox restore on cancel).
   Settings shows the live configured shortcut.
6. **Wider content** — `Page` default width `max-w-3xl` → `max-w-4xl`.

### Open / needs user's eyes (v0.4.0)

- Confirm the **x-margin** fix targeted the right place (I widened dashboard page
  content; if you meant the Cue Card or titlebar, point me at it).
- Decide on the final Cue Card name (I went with "Cue Card"; easy to change).

## v0.4.1 — Cue Card placement & persistence

- Cue Card defaults to the **bottom-right** of the primary display's work area
  (`EDGE_MARGIN` 24px) and is taller: compact `440×460`, expanded `520×680`.
- **Persist geometry**: new `SETTINGS_KEYS.overlayBounds`; `overlayWindow.ts`
  saves `getBounds()` (debounced 400ms) on `move`/`resize`, and restores it on
  creation via `initialBounds()` with an `isOnScreen()` guard (falls back to the
  bottom-right default if the saved monitor is gone). Added to `APP_SETTING_KEYS`
  so factory reset restores the default.
- `setOverlayMode` now anchors the bottom-right corner and clamps to the work
  area, so expanding doesn't push the card off-screen.
- Smoke-tested: clean boot, 0 shortcut-registration failures (earlier failures
  were a leftover dev instance holding the global shortcuts, not a regression).
